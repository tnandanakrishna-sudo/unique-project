#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main()
{
    char str[5],i;
    printf("enter a name/n");
    scanf("%s",str);
    for(int i = 0;str[i]!='\0';i++)
    {
        str[i] = tolower(str[i]);
    }
    printf("%s in lowercase",str);
    return 0;
}
